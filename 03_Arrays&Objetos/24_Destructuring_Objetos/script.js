const people = {
    name: "Lucas",
    idade: 25
}

const {'name' : lname, 'idade' : ldade} =  people;

console.log(lname);

console.log(ldade);

console.log(people);

//Distructuring pega dados de um objeto e transforma em variaveis
