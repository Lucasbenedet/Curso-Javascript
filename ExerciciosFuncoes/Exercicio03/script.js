//Escreva uma função que some dois numeros e retorne eles;
//Depois imprima este retorno;

function soma(x,y) {
    return x + y;
}

// Obs: quando for para retornar tem que usar console log
console.log(soma(10,20));
console.log(soma(33,57));
